.theme-container
  .navbar, .navbar .links
    background-color #2c3e50

.navbar .home-link .site-name
  color white

.nav-links
  .nav-link, .repo-link, .dropdown-title, .nav-link.router-link-active
    color white
  .nav-link:hover, .dropdown-title:hover, .repo-link:hover
    color white

  .dropdown-item .nav-link
    color $textColor

.sidebar-button, .title
  color white

.sidebar .nav-links
  .nav-link, .repo-link, .dropdown-title
    color $textColor

h1, img {
  text-align: center;
  display: block;
  margin-left: auto;
  margin-right: auto;
}
